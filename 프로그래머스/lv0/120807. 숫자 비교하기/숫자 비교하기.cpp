#include <string>
#include <vector>

using namespace std;

int solution(int num1, int num2) {
    switch (num1 == num2) {
        case true:
            return 1;
        case false:
            return -1;
    }
}